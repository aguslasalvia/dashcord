// Shared sizes for the floating tab bar and mini player, so every screen
// that needs to leave space at the bottom of the page (so content doesn't
// hide behind them) uses the same numbers.
export const TAB_BAR_HEIGHT = 72;
export const TAB_BAR_MARGIN = 16;
export const PLAYER_BAR_HEIGHT = 74;
export const FLOATING_GAP = 10; // gap between the mini player and the tab bar
